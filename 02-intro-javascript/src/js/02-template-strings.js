const name = 'Bryan';
const lastName = 'Anariba';
const completeName = `${name} ${lastName}`;
console.log(completeName);

function getSaludo(name) {
  return `${name}`;
}

console.log(getSaludo('Holis!'));